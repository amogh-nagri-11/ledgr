node_modules/
.env
.env.local
.ledgr-state.json
*.log

# Local state snapshots (derived, and only valid for the day they were taken
# because live invoice dates are relative to today)
demo-state.backup.json

# Kept locally, out of the repo (working notes / spec)
LEDGR.md
PROVENANCE.md

# Generated narration for the ?demo=1 autopilot (npm run demo:voice)
public/demo-audio/
